import './mainComponent.scss'
import * as React from 'react';

const MainComponent = () => {
    return <div className="mainComponent">
        <div className="text">here is the simple react template using:</div>
        <div className="desc">webpack, redux, scss, typescript</div>
    </div>
}

export default MainComponent;